/*
    Var:
    - Can be used before it declared
    - Same variable can be declared
    - can be scoped to a function, or globally
    Since ES6, let or const is encouraged to use instead of var, cause:
    - Cannot be used before it is declared
    - Cannot re-declared 
    - It scoped to the block
*/

console.log(user)

var user = "Max";

console.log(user)


